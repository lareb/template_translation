<p id="notice"><%= notice %></p>
<div class="pricing-header px-3 py-3 pt-md-5 pb-md-4 mx-auto text-center">
  <h1 class="display-4">Translation > <%= @template.title %></h1>
  <p class="lead">Please do not refresh or reload the page while translation is in progress.</p>
</div>

<%= render 'form', translation_text: @translation_text %>

<%= link_to 'Back', templates_path %>
